# base-chart

A Helm chart for basic deployments in Kubernetes, with optional Valkey, RabbitMQ, and Cloud SQL Proxy subcharts.

## Install

```bash
helm install my-release oci://ghcr.io/prefeitura-rio/charts/base-chart
```

See `values.yaml` for configuration options.
