{{- range $name, $job := .Values.cronjobs }}
{{- if $job.enabled }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "base-chart.fullname" $ }}-{{ $name }}
  labels:
    {{- include "base-chart.labels" $ | nindent 4 }}
    app.kubernetes.io/component: cronjob
    app.kubernetes.io/cronjob-name: {{ $name }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  schedule: {{ $job.schedule | quote }}
  concurrencyPolicy: {{ $job.concurrencyPolicy | default "Forbid" }}
  successfulJobsHistoryLimit: {{ $job.successfulJobsHistoryLimit | default 3 }}
  failedJobsHistoryLimit: {{ $job.failedJobsHistoryLimit | default 1 }}
  {{- if $job.startingDeadlineSeconds }}
  startingDeadlineSeconds: {{ $job.startingDeadlineSeconds }}
  {{- end }}
  {{- if $job.suspend }}
  suspend: {{ $job.suspend }}
  {{- end }}
  jobTemplate:
    spec:
      {{- if $job.ttlSecondsAfterFinished }}
      ttlSecondsAfterFinished: {{ $job.ttlSecondsAfterFinished }}
      {{- end }}
      {{- if $job.backoffLimit }}
      backoffLimit: {{ $job.backoffLimit }}
      {{- end }}
      {{- if $job.activeDeadlineSeconds }}
      activeDeadlineSeconds: {{ $job.activeDeadlineSeconds }}
      {{- end }}
      template:
        metadata:
          labels:
            {{- include "base-chart.selectorLabels" $ | nindent 12 }}
            app.kubernetes.io/component: cronjob
            app.kubernetes.io/cronjob-name: {{ $name }}
          {{- with $job.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        spec:
          imagePullSecrets:
            {{- include "base-chart.imagePullSecrets" $ | nindent 12 }}
          serviceAccountName: {{ include "base-chart.serviceAccountName" $ }}
          {{- if $.Values.terminationGracePeriodSeconds }}
          terminationGracePeriodSeconds: {{ $.Values.terminationGracePeriodSeconds }}
          {{- end }}
          {{- with $.Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $.Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: {{ $job.restartPolicy | default "OnFailure" }}
          {{- with $job.podSecurityContext | default $.Values.podSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: {{ $name }}
              {{- $image := include "base-chart.image" $ }}
              {{- $imagePullPolicy := $.Values.image.pullPolicy }}
              {{- if $job.image }}
              {{- $repo := $job.image.repository | default $.Values.image.repository }}
              {{- $tag := $job.image.tag | default $.Values.image.tag | default "latest" }}
              {{- $image = printf "%s:%s" $repo $tag }}
              {{- $imagePullPolicy = $job.image.pullPolicy | default $imagePullPolicy }}
              {{- end }}
              image: {{ $image | quote }}
              imagePullPolicy: {{ $imagePullPolicy }}
              {{- with $job.command }}
              command:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- with $job.args }}
              args:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- $mountAsEnv := ternary $.Values.configMap.mountAsEnv true (hasKey $.Values.configMap "mountAsEnv") }}
              {{- $configMapEnv := and $.Values.configMap.enabled $mountAsEnv }}
              {{- if or $job.envFrom $.Values.envFrom $.Values.infisicalSecret.enabled $configMapEnv }}
              envFrom:
                {{- if $.Values.infisicalSecret.enabled }}
                - secretRef:
                    name: {{ $.Values.infisicalSecret.secretName }}
                {{- end }}
                {{- if $configMapEnv }}
                - configMapRef:
                    name: {{ include "base-chart.fullname" $ }}
                {{- end }}
                {{- with $job.envFrom }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
                {{- with $.Values.envFrom }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
              {{- end }}
              {{- if or $job.env $.Values.valkey.enabled $.Values.rabbitmq.enabled }}
              env:
                {{- with $job.env }}
                {{- toYaml . | nindent 16 }}
                {{- end }}
                {{- if $.Values.valkey.enabled }}
                - name: REDIS_HOST
                  value: {{ $.Release.Name }}-valkey
                - name: REDIS_PORT
                  value: "6379"
                {{- end }}
                {{- if $.Values.rabbitmq.enabled }}
                - name: RABBITMQ_HOST
                  value: {{ $.Release.Name }}-rabbitmq
                - name: RABBITMQ_PORT
                  value: {{ $.Values.rabbitmq.service.amqpPort | quote }}
                - name: RABBITMQ_USER
                  value: {{ $.Release.Name }}
                {{- end }}
              {{- end }}
              {{- with $job.resources | default $.Values.resources }}
              resources:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- with $job.securityContext | default $.Values.securityContext }}
              securityContext:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              {{- with $job.volumeMounts | default $.Values.volumeMounts }}
              volumeMounts:
                {{- toYaml . | nindent 16 }}
              {{- end }}
          {{- with $job.volumes | default $.Values.volumes }}
          volumes:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
{{- end }}
