# typesense

Helm chart for [Typesense](https://typesense.org/), a fast, typo-tolerant search engine.

## Install

```bash
helm install my-release oci://ghcr.io/prefeitura-rio/charts/typesense
```

See `values.yaml` for configuration options.
