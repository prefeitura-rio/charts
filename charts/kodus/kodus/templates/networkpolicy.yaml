{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kodus.fullname" . }}-default-deny
  labels:
    {{- include "kodus.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/part-of: kodus
      app.kubernetes.io/instance: {{ .Release.Name }}
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  {{- if .Values.networkPolicy.egressRules }}
  egress:
    {{- toYaml .Values.networkPolicy.egressRules | nindent 4 }}
  {{- else }}
  egress:
    - {}
  {{- end }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "kodus.fullname" . }}-allow-intra-app
  labels:
    {{- include "kodus.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/part-of: kodus
      app.kubernetes.io/instance: {{ .Release.Name }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/part-of: kodus
              app.kubernetes.io/instance: {{ .Release.Name }}
{{- range $name := (list "web" "api" "webhooks") }}
{{- $svc := index $.Values.services $name }}
{{- if and $svc (or (kindIs "invalid" $svc.enabled) $svc.enabled) $svc.port }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-%s-ingress" (include "kodus.fullname" $) $name }}
  labels:
    {{- include "kodus.labels" $ | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "kodus-common.serviceSelectorLabels" (dict "root" $ "serviceName" $name) | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    {{- if and $.Values.networkPolicy.ingressControllerLabels $.Values.networkPolicy.ingressControllerNamespaceSelector }}
    - from:
        - namespaceSelector:
            {{- toYaml ($.Values.networkPolicy.ingressControllerNamespaceSelector | default dict) | nindent 12 }}
          podSelector:
            matchLabels:
              {{- toYaml $.Values.networkPolicy.ingressControllerLabels | nindent 14 }}
        {{- range $.Values.networkPolicy.additionalIngressSources }}
        - namespaceSelector:
            {{- toYaml .namespaceSelector | nindent 12 }}
          podSelector:
            {{- toYaml .podSelector | nindent 12 }}
        {{- end }}
      ports:
        - port: {{ $svc.port }}
          protocol: TCP
    {{- end }}
{{- end }}
{{- end }}
{{- end }}
